import React from "react";

const Settings = () => {
    return (
        <div id="font">
            <p> If I allowed you to tamper with the settings, this is the page that you would do so.</p>
        </div>
    )
};

export default Settings;